# WeKnora 本体约束实体图抽取补充提示词

该提示词由 `ontology.CompileWeKnora` 动态生成具体类型、属性和关系约束。本文件只定义固定原则：

1. 实体只能使用本体允许的业务类型，并在 attributes 中输出 `__type__=类型名`。
2. 关系方向必须满足本体 Domain/Range。
3. 属性必须属于实体类型允许的属性集合。
4. 不允许根据常识补全原文未出现的属性或关系。
5. 无法映射的新概念保留为候选，不强行塞入现有类型。
6. 所有实体、属性、关系必须可回溯到当前 Chunk 或明确的相邻上下文。
